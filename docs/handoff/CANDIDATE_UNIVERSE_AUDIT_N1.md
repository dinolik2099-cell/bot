# Candidate Universe Audit N1

Baseline: `a00d3f22fb900327f73d5d8fe688e66ceb95d957`.

The current registry resolves to 36 distinct registered models. This is already
within the predeclared 30–50 target, so N1 adds no strategy implementation and
does not consume any research window. The N1 contract creates stable IDs and
deterministic identity hashes for the existing registered universe only.

The universe remains candidate-only. `OOS = SEALED`; this audit neither reads
nor summarizes formal OOS artifacts.
