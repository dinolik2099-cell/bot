# V1.3.4 变更清单

1. 增加 `--mode quick|full`。
2. 默认模式改为 `quick`，避免无参数执行时直接长跑。
3. quick 最多约 5 分钟、最多 1 轮，仅覆盖 MODEL_4_X6、TRAIN/VALIDATION、基准与手续费×3+滑点×10。
4. quick 跳过 Monte Carlo、7日 Block Bootstrap、多方向同步冲击。
5. full 模式保持 V1.3.3 的完整压力测试范围；需要显式 `--mode full`。
6. 修正输出文件名和成功标记为 V1.3.4。
7. 增加默认短测/显式长测回归测试。
